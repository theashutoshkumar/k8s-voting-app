apiVersion: v1
kind: Pod 
metadata:
  name: voting-app
  labels:
    name: voting-app
    app: demo-voting-app 
spec:
  containers: 
  - name: voting-app
    image: kodecloud/example-voting-app_vote:v1
    ports:
    - containerPort: 80